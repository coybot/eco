# Large demo video — too big for GitHub (>100 MB). See CLAUDE.md for setup.
/public/media/fleet-demo.mp4

# onnxruntime-web's wasm runtime (~13MB) — fully derived from the pinned
# onnxruntime-web dependency in package.json/package-lock.json, regenerated by
# `npm run sync-ort` (also runs automatically via postinstall/prebuild). Not
# committed to avoid vendoring a large binary that's already in node_modules.
/public/ort/
