{
  "name": "presidio-drone-api",
  "private": true,
  "scripts": {
    "deploy": "sam build && sam deploy",
    "logs": "sam logs --stack-name presidio-drone-api --tail"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "typescript": "^5.7.3"
  }
}
